import React from 'react';
import { hot } from 'react-hot-loader/root';
import MainText from 'components/MainText';

const Login = () => (
  <section className="container">
    <div className="box">
      <MainText>This is stub for Login screen</MainText>
    </div>
  </section>
);

export default hot(Login);
